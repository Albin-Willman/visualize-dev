<% content_for :content do %>
  <section>
    <div class="container">
      <h1><%= "New Feature in #{@project.name}" %></h1>

      <%= render 'form' %>

      <%= link_to 'Back', project_path(@project) %>
    </div>
  </section>
<% end %>